/*
@ama
let nd_root=ParseCurrentFile();
nd_root.Validate();
console.log(JSON.stringify(nd_root,null,2))
console.log(nd_root.toSource())
*/

template<>
class TestClass<int>:Base{
	union TestUnion extends Base{
		enum foo{
			bar=3;
		}
	}
	int fwdMethod(void)const;
};

WEIRD_DECL_MACRO(int,weirdForward)(int p,int q=3);

Node.JSVariadic=function(arg0,...rest){
	return rest;
}

int main(int argc,char** argv){
	int uninitialized_var;
	auto a=function(int p=3,...){
		return p+p;
	}
	if(a)c+=a;else c+=b;
	if(size)(*size)=dialog->GetSize();
	if(size)--size;
	c+=a*b;
	do{
		console.log('do-while');
	}while(0);
	console.log('after do-while');
	return 0xabcd;
}
