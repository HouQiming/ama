#include <stdio.h>

/*
@ama
ParseCurrentFile()
	.then(require('cpp/sane_types'))
	.Save('.audit.cpp')
	.then(require('cpp/sane_types').inverse)
	.Save('.aba.audit.cpp')
*/

int[] test(char[:] a,uint32_t[]*[:]& b){
	int[] a;
	float[9] b;
	Map<char[],double> c;
	return 0;
}
