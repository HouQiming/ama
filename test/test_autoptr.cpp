/*
@ama
const amacpp=require('cpp/amacpp');
console.log(ParseCurrentFile({parse_indent_as_scope:1}).then(amacpp).toSource());
*/
int main() {
	TokenWithPrediction*[] tbf_tokens_ptrs;
	answers.push_back(tbf_tokens_ptrs[i].token);
}
