// //// JC version: 3
#include "./charset.jch"

namespace ama {
	char*! SkipChars(char*! feed, uint32_t[...]! cset) {
		while( ama::isInCharSet(cset, uint32_t(uint8_t(feed[0L]))) ) {
			feed += 1;
		}
		return feed;
	}
};
